# ROADMAP

## Near-term
- Add more realistic synthetic fixtures (optional packs).
- Add opt-in fault injection for demos/tests (latency + failure simulation; env-guarded).
- Add guarded reset endpoint to restore seeded demo data for deterministic demos.
- Add Docker Compose for local demos (db volume + API) and “safe demo” guidance.

## Later
- Add synthetic file attachments metadata.
- Add ingest API for external fixtures.
